{
  "name": "cinemaxapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd cinemaX && npm install && npm run build",
    "vercel-build": "cd cinemaX && npm install && npm run build"
  }
}
